# ASCENDER_HTTP_MODE=redirect (default): plain http only redirects to https.
# If ASCENDER_HTTPS_PORT is not 443, add the port here: https://$host:8443$request_uri
return 301 https://$host$request_uri;
