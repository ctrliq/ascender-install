[defaults]
inventory = inventory
roles_path = roles
retry_files_enabled = False
#stdout_callback = yaml
interpreter_python = auto_silent
# The UI build and pip compile take a while; keep the connection alive.
timeout = 60

[ssh_connection]
pipelining = True
